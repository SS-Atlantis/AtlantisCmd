name: Add Milestone to Issue/PR

permissions: {}

on:
  issues:
    types:
      - opened
  pull_request:
    types:
      - opened
    branches:
      - main

jobs:
  add_milestone:
    name: Add Milestone to Issue/PR
    permissions:
      issues: write  # Required to add current milestone to issue
      pull-requests: write  # Required to add current milestone to PR
    uses: UBC-MOAD/gha-workflows/.github/workflows/auto-milestone-issue-pr.yaml@main  # zizmor: ignore[unpinned-uses]
