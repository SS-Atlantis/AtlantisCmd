# conda environment description file for readthedocs build environment

name: atlantis-cmd-rtd

channels:
  - conda-forge
  - nodefaults

dependencies:
  - pip
  - python=3.11

  # RTD packages
  - mock
  - pillow
  - sphinx
  - sphinx_rtd_theme
